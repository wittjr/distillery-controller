type: object
description: ''
x-examples:
  example-1:
    still:
      id: string
      type: fractional
      status: free
      phidgets:
        - name: string
          type: temperature-probe
properties:
  still:
    $ref: ./still.v1.yaml
  stipping-run-required:
    type: boolean
